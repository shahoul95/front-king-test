export class IKingfrench  {
  id: number;
  name: string;
  date_of_dead:number;
  date_of_birth:number;
  constructor(
    id,
    name,
    date_of_dead,
    date_of_birth,
  ) {
    this.id = id;
    this.name = name;
    this.date_of_dead = date_of_dead;
    this.date_of_birth = date_of_birth;
  }
}

